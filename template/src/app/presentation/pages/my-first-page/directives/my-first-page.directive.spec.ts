import { MyFirstPageDirective } from './my-first-page.directive';

describe('MyFirstPageDirective', () => {
  it('should create an instance', () => {
    const directive = new MyFirstPageDirective();
    expect(directive).toBeTruthy();
  });
});
