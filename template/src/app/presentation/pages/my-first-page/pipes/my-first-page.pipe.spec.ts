import { MyFirstPagePipe } from './my-first-page.pipe';

describe('MyFirstPagePipe', () => {
  it('create an instance', () => {
    const pipe = new MyFirstPagePipe();
    expect(pipe).toBeTruthy();
  });
});
